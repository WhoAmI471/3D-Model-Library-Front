# --- Application ---
NODE_ENV=development
PORT=3000
JWT_SECRET=please_change_me_to_a_long_random_string

# --- Database (PostgreSQL for Prisma 7) ---
# If you run the app on the host machine (not inside docker), use localhost:
DATABASE_URL=postgresql://postgres:qwerty@localhost:5432/3d_library?schema=public

# --- Nextcloud (Docker) ---
# Nextcloud container exposes 80 -> 8080 on host by default in docker-compose.yml
# If your Next.js app runs on the host (not in Docker), use localhost:
NEXTCLOUD_URL=http://localhost:8080
# If your Next.js app runs in Docker (same network as nextcloud service), use service name:
# NEXTCLOUD_URL=http://nextcloud:80
# Or set NEXTCLOUD_DOCKER_SERVICE to automatically use service name when URL contains localhost:
# NEXTCLOUD_DOCKER_SERVICE=nextcloud
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=yourpassword

# Optional: tune Prisma logs (debug/noise)
# LOG_LEVEL=info
# PRISMA_CLIENT_ENGINE_TYPE=binary

DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
NEXTCLOUD_URL=https://nextcloud.example.com
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=password
JWT_SECRET=your-secret-key
